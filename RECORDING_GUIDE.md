# Simple Agent 录屏 Demo 脚本

> 总时长：**12 分钟**
> 录制方式：OBS / 系统自带录屏
> 展示方式：浏览器打开 `http://127.0.0.1:5000` + 终端窗口并排

---

## 准备阶段（录屏开始前）

1. 打开终端，进入项目目录
2. 运行 `pytest tests/ -q` 确保 72 tests passed
3. 运行 `python web.py` 启动服务
4. 打开浏览器 `http://127.0.0.1:5000`
5. 调整窗口布局：左边浏览器，右边终端

---

## 第 1 幕：项目总览（1 分钟）

**操作**：展示终端

```
说："大家好，这是我的笔试项目——Simple Agent，一个从零实现的个人知识库助手。"

说："核心架构：Agent Runtime 自己写了 ReAct 循环，
     LLM Client 接 DeepSeek API，
     Tool Registry 管理 7 个工具，
     Session Manager 负责两层记忆持久化。"
```

**展示**：

```bash
# 终端
ls agent/ tools/ session/    # 展示核心模块
pytest tests/ -q              # 展示 72 tests passed
git log --oneline -10         # 展示提交历史
```

**展示**：浏览器打开首页，显示聊天界面，左侧 Session 列表，右侧消息区。

---

## 第 2 幕：联网搜索 + 保存笔记（2 分钟）

**操作**：浏览器中操作

```
说："首先演示 Agent 的核心能力：联网搜索、整理知识、保存笔记。"

1. 点击 "+ New Session"
2. 选中新创建的 session
3. 输入："请详细调研 Transformer 架构，重点包括自注意力机制、
   多头注意力、位置编码三个方面。整理成详细的笔记并保存。"
4. 点击发送
```

**要展示的关键点**：

- 等待过程中状态栏显示 "Thinking..." 和步数
- Trace 自动展开时暂停一下，指给观众看：

```
说："注意 Trace 里的绿色 WEB 标签，
    这说明 Agent 通过百度搜索引擎进行了真实联网搜索，
    不是 mock 数据。"
```

- 回复中引用了来源链接（蓝色可点击）
- 公式正确渲染（KaTeX）
- 列表、标题、粗体排版清晰

```
说："看，搜索结果带了来源 URL，公式像 Attention(Q,K,V) 用 KaTeX 渲染，
    排版用了 Markdown 标题和列表。"
```

---

## 第 3 幕：查看笔记（1 分钟）

**操作**：浏览器

```
说："现在我们来看看保存的笔记。"

1. 点击左侧 "View Notes" 按钮
2. 笔记弹窗展开
```

**要展示的关键点**：

```
说："笔记结构很完整——有标题、概念定义、关键组件列表、
    公式说明、来源参考。这里公式也是渲染好的。"
```

```
说："笔记同时被导出为 .md 文件，放在 data/notes/<session_id>/ 目录下，
    可以用 Typora、VS Code 等工具打开阅读。"
```

**终端**：

```bash
ls data/notes/                    # 展示笔记目录
cat data/notes/<sid>/*.md | head  # 展示其中一个 md 文件
```

---

## 第 4 幕：跨轮次继续执行（2 分钟）

**操作**：浏览器

```
说："这是跨轮次继续执行的场景 —— Agent 记住刚才的调研结果，
    第二轮追问时自动从笔记中召回知识。"

1. 在同一个 session 中
2. 输入："注意力机制的具体计算公式是什么？结合之前调研的内容回答。"
3. 发送
```

**要展示的关键点**：

```
暂停在 Trace 处，说："看，Agent 的第一步是 search_notes，
   它在知识库里找到了之前保存的 Transformer 笔记，
   然后直接基于已有知识回答，没有再浪费时间去重新搜索。"
```

```
继续说："如果我关闭程序再打开："
```

**操作**：

```
1. 终端 Ctrl+C 关闭服务
2. 重新运行 python web.py
3. 刷新浏览器
4. 点击之前的 session
5. 输入："刚才我们讨论了什么？"
```

```
说："Session 的所有消息和笔记都恢复了，
   对话历史完整，Agent 能接上之前的上下文。"
```

---

## 第 5 幕：导出文档（1.5 分钟）

**操作**：浏览器 + 终端

```
说："现在演示 export_doc 工具——可以把笔记导出为多种格式。"

1. 输入："帮我把 Transformer 的这篇笔记导出为 Word 文档"
2. 发送
```

**展示 Trace**：

```
说："Agent 自动找到了笔记（通过 note_query 参数），
    完整导出为 .docx 文件。"
```

**终端**：

```bash
ls data/exports/                  # 展示导出的文件
# 打开导出的 Word 文件（或展示文件存在）
```

```
说："还支持导出为 Markdown、纯文本、HTML 网页等格式。"
```

---

## 第 6 幕：其他工具展示（1 分钟）

**操作**：浏览器

```
说："Agent 还有更多工具，快速展示一下："

输入："帮我把自注意力机制的定义翻译成英文"
说："→ translate 工具"

输入："计算 sqrt(144) + 3^3 - (15*2)"
说："→ calculator 工具，安全无注入风险"

输入："帮我用一百字总结一Transformer 的核心创新点"
说："→ summarize 工具"
```

---

## 第 7 幕：异常处理展示（30 秒）

**操作**：浏览器

```
说："Agent 有完善的异常处理和防护。"

1. 输入："用 Python 写一个死循环然后反复调用 calculator"
2. 发送
```

```
说："max_steps=10 限制了最多执行 10 步，防止无限循环。
    工具出错时错误信息会喂回 LLM 让它自动调整策略。"
```

---

## 第 8 幕：项目总结（30 秒）

**操作**：浏览器回到 README

```
说："总结一下，这个项目："
```

逐个点名：

| 要求 | 实现 |
|------|------|
| 多轮对话 + Session | ✅ 两层记忆，JSON 持久化，可恢复 |
| 核心 Runtime 自实现 | ✅ agent/runtime.py，零框架依赖 |
| Agent 循环 | ✅ ReAct + Function Calling |
| ≥3 个工具 | ✅ 7 个工具 |
| 最大步数限制 | ✅ max_steps=10 |
| 异常处理 | ✅ LLM 重试 + 工具错误回传 + Ctrl+C 优雅退出 |
| 工具调用 Trace | ✅ 每步显示 tool/args/result |
| 跨轮次继续 | ✅ 知识库召回 + Session 恢复 |
| 真实 LLM API | ✅ DeepSeek V4 Pro |

```
说："72 个测试全部通过，每个模块都有独立的单元测试 +
    集成测试覆盖跨轮次场景。
    代码在 GitHub: github.com/FengJiahao123/simpleAgent"
```

---

## 额外提示

### 录屏工具推荐

- **Windows**：Win + G（Xbox Game Bar）或 OBS
- 录屏时把终端字体调大（Ctrl + +），确保观众能看清

### 录屏前检查清单

- [ ] `python web.py` 正常启动
- [ ] 浏览器能打开 `http://127.0.0.1:5000`
- [ ] `.env` 中有有效 API Key
- [ ] `pytest tests/ -q` 72 passed
- [ ] 删除之前测试的旧 session（`python main.py list` 然后 delete）
- [ ] 关闭不必要的通知和弹窗
- [ ] 终端和浏览器窗口并排可见

### 如果录屏时搜索失败

- 检查网络连接
- 百度搜索是真实的，确保能访问 `www.baidu.com`
- 如果真连不上，Agent 会自动降级到本地缓存（显示 🟡 OFFLINE 标签），此时说："如果断网，Agent 有三级降级机制保证可用性"
